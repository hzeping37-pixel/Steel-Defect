# 阶段四：功能迁移和集成计划

## 开发目标
完成前后端联调、功能测试、性能优化、部署配置和文档完善，确保系统可以正常运行。

## 开发任务

### 1. 前后端联调 (2-3天)

#### 1.1 API接口对接
- 测试所有API接口
- 修复接口兼容性问题
- 优化API响应格式
- 处理错误情况

#### 1.2 数据格式验证
- 请求数据格式验证
- 响应数据格式验证
- 数据类型转换
- 空值处理

#### 1.3 认证流程测试
- 登录流程测试
- 注册流程测试
- 令牌刷新测试
- 权限验证测试

#### 1.4 文件上传测试
- 图片上传测试
- 文件大小限制
- 文件类型验证
- 上传进度显示

### 2. 功能测试 (2-3天)

#### 2.1 用户功能测试
- 用户注册
- 用户登录
- 个人信息管理
- 密码修改

#### 2.2 检测功能测试
- 图片检测
- 摄像头检测
- 参数调节
- 结果展示

#### 2.3 记录功能测试
- 记录列表
- 记录详情
- 记录搜索
- 记录删除

#### 2.4 管理员功能测试
- 看板统计
- 用户管理
- 记录管理
- 数据统计

### 3. 性能优化 (1-2天)

#### 3.1 前端性能优化
- 代码分割优化
- 图片懒加载
- 组件缓存
- 请求优化

#### 3.2 后端性能优化
- API响应优化
- 数据库查询优化
- 缓存策略
- 异步处理

#### 3.3 网络性能优化
- 请求压缩
- 响应缓存
- CDN配置
- 负载均衡

### 4. 部署配置 (1-2天)

#### 4.1 环境配置
- 生产环境变量
- 数据库配置
- 缓存配置
- 日志配置

#### 4.2 Docker部署
- 镜像构建
- 容器编排
- 网络配置
- 存储配置

#### 4.3 Nginx配置
- 反向代理
- SSL证书
- 静态文件服务
- 负载均衡

#### 4.4 监控配置
- 健康检查
- 性能监控
- 错误监控
- 日志收集

### 5. 文档完善 (1天)

#### 5.1 用户文档
- 安装指南
- 使用说明
- 常见问题
- 联系方式

#### 5.2 开发文档
- API文档
- 架构说明
- 部署指南
- 贡献指南

#### 5.3 运维文档
- 监控告警
- 故障排查
- 备份恢复
- 升级指南

## 技术实现

### 1. 前后端联调

#### API测试脚本
```python
# test_api.py
import requests
import json

BASE_URL = "http://localhost:8000"

def test_login():
    """测试登录接口"""
    response = requests.post(f"{BASE_URL}/api/auth/login", json={
        "username": "admin",
        "password": "admin123"
    })
    assert response.status_code == 200
    data = response.json()
    assert "access_token" in data
    return data["access_token"]

def test_detection(token):
    """测试检测接口"""
    headers = {"Authorization": f"Bearer {token}"}
    with open("test_image.jpg", "rb") as f:
        response = requests.post(
            f"{BASE_URL}/api/detection/image",
            headers=headers,
            files={"file": f},
            data={"conf_threshold": 0.25, "iou_threshold": 0.45}
        )
    assert response.status_code == 200
    data = response.json()
    assert "batch_id" in data
    return data

def test_records(token):
    """测试记录接口"""
    headers = {"Authorization": f"Bearer {token}"}
    response = requests.get(f"{BASE_URL}/api/records/", headers=headers)
    assert response.status_code == 200
    data = response.json()
    assert isinstance(data, list)
    return data
```

#### 前端测试组件
```tsx
// TestComponent.tsx
import React, { useState } from 'react';
import { Button, message } from 'antd';
import { authAPI, detectionAPI, recordsAPI } from '../services/api';

const TestComponent: React.FC = () => {
  const [loading, setLoading] = useState(false);

  const runTests = async () => {
    setLoading(true);
    try {
      // 测试登录
      const loginResult = await authAPI.login({
        username: 'admin',
        password: 'admin123'
      });
      console.log('登录测试通过:', loginResult);
      message.success('登录测试通过');

      // 测试获取用户信息
      const userInfo = await authAPI.getMe();
      console.log('用户信息测试通过:', userInfo);
      message.success('用户信息测试通过');

      // 测试获取记录
      const records = await recordsAPI.getRecords(10, 0);
      console.log('记录列表测试通过:', records);
      message.success('记录列表测试通过');

      message.success('所有测试通过！');
    } catch (error) {
      console.error('测试失败:', error);
      message.error('测试失败');
    } finally {
      setLoading(false);
    }
  };

  return (
    <Button loading={loading} onClick={runTests}>
      运行测试
    </Button>
  );
};

export default TestComponent;
```

### 2. 性能优化

#### 前端代码分割
```tsx
// 路由级别代码分割
import React, { Suspense, lazy } from 'react';
import { Spin } from 'antd';

const Dashboard = lazy(() => import('./pages/Dashboard/Dashboard'));
const Detection = lazy(() => import('./pages/Detection/Detection'));
const Records = lazy(() => import('./pages/Records/Records'));
const Admin = lazy(() => import('./pages/Admin/Admin'));

const App: React.FC = () => {
  return (
    <Suspense fallback={<Spin size="large" />}>
      <Routes>
        <Route path="/" element={<Dashboard />} />
        <Route path="/detection" element={<Detection />} />
        <Route path="/records" element={<Records />} />
        <Route path="/admin/*" element={<Admin />} />
      </Routes>
    </Suspense>
  );
};
```

#### 后端缓存策略
```python
# cache.py
from functools import lru_cache
from datetime import datetime, timedelta

class CacheManager:
    def __init__(self):
        self.cache = {}
        self.ttl = {}
    
    def get(self, key: str):
        """获取缓存"""
        if key in self.cache:
            if datetime.now() < self.ttl.get(key, datetime.min):
                return self.cache[key]
            else:
                del self.cache[key]
                del self.ttl[key]
        return None
    
    def set(self, key: str, value: any, ttl_seconds: int = 300):
        """设置缓存"""
        self.cache[key] = value
        self.ttl[key] = datetime.now() + timedelta(seconds=ttl_seconds)
    
    def clear(self):
        """清除缓存"""
        self.cache.clear()
        self.ttl.clear()

# 使用示例
cache = CacheManager()

async def get_user_records(user_id: str):
    cache_key = f"user_records:{user_id}"
    cached = cache.get(cache_key)
    if cached:
        return cached
    
    records = await fetch_records_from_db(user_id)
    cache.set(cache_key, records, ttl_seconds=60)
    return records
```

### 3. 部署配置

#### Docker Compose生产配置
```yaml
# docker-compose.prod.yml
version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    environment:
      - DEBUG=False
      - SECRET_KEY=${SECRET_KEY}
      - CHROMADB_HOST=chromadb
      - REDIS_HOST=redis
    depends_on:
      - chromadb
      - redis
    restart: always
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G

  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
    restart: always
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G

  chromadb:
    image: chromadb/chroma:latest
    volumes:
      - chromadb_data:/chroma/chroma
    restart: always
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 2G

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./docker/nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - backend
      - frontend
    restart: always

volumes:
  chromadb_data:
  redis_data:
```

#### Nginx生产配置
```nginx
# nginx.prod.conf
events {
    worker_connections 1024;
}

http {
    upstream backend {
        server backend:8000;
    }

    upstream frontend {
        server frontend:3000;
    }

    server {
        listen 80;
        server_name your-domain.com;
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl;
        server_name your-domain.com;

        ssl_certificate /etc/nginx/ssl/cert.pem;
        ssl_certificate_key /etc/nginx/ssl/key.pem;

        # 安全头
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;

        # 前端
        location / {
            proxy_pass http://frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # 后端API
        location /api/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # WebSocket
        location /ws/ {
            proxy_pass http://backend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        # 静态文件缓存
        location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
            proxy_pass http://frontend;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
}
```

### 4. 监控配置

#### 健康检查端点
```python
# health.py
from fastapi import APIRouter
import psutil
import torch

router = APIRouter()

@router.get("/health")
async def health_check():
    """系统健康检查"""
    return {
        "status": "healthy",
        "timestamp": datetime.now().isoformat(),
        "services": {
            "database": check_database(),
            "model": check_model(),
            "storage": check_storage()
        }
    }

@router.get("/health/detailed")
async def detailed_health_check():
    """详细健康检查"""
    return {
        "status": "healthy",
        "timestamp": datetime.now().isoformat(),
        "system": {
            "cpu_percent": psutil.cpu_percent(),
            "memory_percent": psutil.virtual_memory().percent,
            "disk_percent": psutil.disk_usage('/').percent
        },
        "gpu": {
            "available": torch.cuda.is_available(),
            "device_count": torch.cuda.device_count() if torch.cuda.is_available() else 0,
            "memory_allocated": torch.cuda.memory_allocated() if torch.cuda.is_available() else 0
        },
        "services": {
            "database": check_database(),
            "model": check_model(),
            "storage": check_storage()
        }
    }
```

#### 日志配置
```python
# logging_config.py
import logging
from logging.handlers import RotatingFileHandler
import sys

def setup_logging():
    """配置日志"""
    # 创建日志格式
    formatter = logging.Formatter(
        '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
    )
    
    # 控制台处理器
    console_handler = logging.StreamHandler(sys.stdout)
    console_handler.setFormatter(formatter)
    
    # 文件处理器
    file_handler = RotatingFileHandler(
        'logs/app.log',
        maxBytes=10*1024*1024,  # 10MB
        backupCount=5
    )
    file_handler.setFormatter(formatter)
    
    # 配置根日志记录器
    root_logger = logging.getLogger()
    root_logger.setLevel(logging.INFO)
    root_logger.addHandler(console_handler)
    root_logger.addHandler(file_handler)
    
    # 配置特定模块日志
    logging.getLogger("uvicorn").setLevel(logging.WARNING)
    logging.getLogger("sqlalchemy").setLevel(logging.WARNING)
```

## 测试策略

### 1. 单元测试

#### 后端测试
```python
# test_auth.py
import pytest
from fastapi.testclient import TestClient
from main import app

client = TestClient(app)

def test_login_success():
    """测试登录成功"""
    response = client.post("/api/auth/login", json={
        "username": "admin",
        "password": "admin123"
    })
    assert response.status_code == 200
    data = response.json()
    assert "access_token" in data
    assert "refresh_token" in data

def test_login_failure():
    """测试登录失败"""
    response = client.post("/api/auth/login", json={
        "username": "admin",
        "password": "wrong_password"
    })
    assert response.status_code == 401

def test_register_success():
    """测试注册成功"""
    response = client.post("/api/auth/register", json={
        "username": "test_user",
        "password": "test123",
        "user_type": "personal"
    })
    assert response.status_code == 200
    data = response.json()
    assert data["username"] == "test_user"
```

#### 前端测试
```tsx
// AuthContext.test.tsx
import { renderHook, act } from '@testing-library/react-hooks';
import { AuthProvider, useAuth } from './AuthContext';

describe('AuthContext', () => {
  it('should login successfully', async () => {
    const { result } = renderHook(() => useAuth(), {
      wrapper: AuthProvider,
    });

    await act(async () => {
      const success = await result.current.login('admin', 'admin123');
      expect(success).toBe(true);
    });

    expect(result.current.isAuthenticated).toBe(true);
    expect(result.current.user).toBeTruthy();
  });

  it('should logout successfully', async () => {
    const { result } = renderHook(() => useAuth(), {
      wrapper: AuthProvider,
    });

    await act(async () => {
      await result.current.login('admin', 'admin123');
    });

    act(() => {
      result.current.logout();
    });

    expect(result.current.isAuthenticated).toBe(false);
    expect(result.current.user).toBeNull();
  });
});
```

### 2. 集成测试

#### API集成测试
```python
# test_integration.py
import pytest
import requests
import json

BASE_URL = "http://localhost:8000"

class TestIntegration:
    def __init__(self):
        self.token = None
    
    def setup(self):
        """测试前准备"""
        # 登录获取令牌
        response = requests.post(f"{BASE_URL}/api/auth/login", json={
            "username": "admin",
            "password": "admin123"
        })
        assert response.status_code == 200
        self.token = response.json()["access_token"]
    
    def test_full_workflow(self):
        """测试完整工作流程"""
        headers = {"Authorization": f"Bearer {self.token}"}
        
        # 1. 获取用户信息
        response = requests.get(f"{BASE_URL}/api/auth/me", headers=headers)
        assert response.status_code == 200
        
        # 2. 获取记录列表
        response = requests.get(f"{BASE_URL}/api/records/", headers=headers)
        assert response.status_code == 200
        
        # 3. 获取统计信息
        response = requests.get(f"{BASE_URL}/api/records/statistics/summary", headers=headers)
        assert response.status_code == 200
        
        # 4. 获取管理员看板（如果是管理员）
        if response.json().get("user_type") == "admin":
            response = requests.get(f"{BASE_URL}/api/admin/dashboard", headers=headers)
            assert response.status_code == 200
```

### 3. 性能测试

#### 负载测试
```python
# test_performance.py
import asyncio
import aiohttp
import time

async def test_concurrent_requests():
    """测试并发请求"""
    url = "http://localhost:8000/api/records/"
    headers = {"Authorization": "Bearer test_token"}
    
    async with aiohttp.ClientSession() as session:
        tasks = []
        for i in range(100):
            task = session.get(url, headers=headers)
            tasks.append(task)
        
        start_time = time.time()
        responses = await asyncio.gather(*tasks)
        end_time = time.time()
        
        success_count = sum(1 for r in responses if r.status == 200)
        total_time = end_time - start_time
        
        print(f"总请求数: {len(responses)}")
        print(f"成功请求数: {success_count}")
        print(f"总时间: {total_time:.2f}秒")
        print(f"平均响应时间: {total_time/len(responses)*1000:.2f}毫秒")
        print(f"QPS: {len(responses)/total_time:.2f}")

# 运行测试
asyncio.run(test_concurrent_requests())
```

## 部署流程

### 1. 环境准备

```bash
# 1. 安装Docker和Docker Compose
# 2. 配置环境变量
cp backend/.env.example backend/.env
# 编辑 .env 文件，填入生产环境配置

# 3. 准备SSL证书（可选）
mkdir -p ssl
# 将证书文件放到 ssl 目录
```

### 2. 构建和部署

```bash
# 1. 构建镜像
docker-compose -f docker-compose.prod.yml build

# 2. 启动服务
docker-compose -f docker-compose.prod.yml up -d

# 3. 查看服务状态
docker-compose -f docker-compose.prod.yml ps

# 4. 查看日志
docker-compose -f docker-compose.prod.yml logs -f
```

### 3. 数据迁移

```bash
# 1. 备份现有数据
cp -r data/chromadb data/chromadb_backup

# 2. 初始化数据库
docker-compose -f docker-compose.prod.yml exec backend python init_db.py

# 3. 导入数据（如果有）
# 根据实际情况执行数据导入脚本
```

### 4. 监控和维护

```bash
# 1. 查看系统状态
curl http://localhost:8000/health

# 2. 查看详细状态
curl http://localhost:8000/health/detailed

# 3. 查看资源使用
docker stats

# 4. 查看日志
docker-compose -f docker-compose.prod.yml logs -f backend
```

## 时间安排

| 任务 | 时间 | 交付物 |
|------|------|--------|
| 前后端联调 | 2-3天 | 接口测试报告 |
| 功能测试 | 2-3天 | 功能测试报告 |
| 性能优化 | 1-2天 | 性能优化报告 |
| 部署配置 | 1-2天 | 部署文档 |
| 文档完善 | 1天 | 完整文档 |

**总计**: 7-11天

## 风险评估

### 1. 技术风险

- **接口兼容性**：前后端数据格式不匹配
- **性能瓶颈**：高并发时系统响应慢
- **部署问题**：Docker配置错误

### 2. 业务风险

- **功能缺失**：迁移过程中遗漏功能
- **数据丢失**：迁移过程中数据丢失
- **用户体验**：新系统使用体验差

### 3. 缓解措施

- **充分测试**：每个功能都进行测试
- **数据备份**：迁移前备份所有数据
- **灰度发布**：逐步迁移用户
- **回滚方案**：准备回滚计划

## 成功标准

### 1. 功能标准

- ✅ 所有API接口正常工作
- ✅ 所有功能正常运行
- ✅ 用户认证系统正常
- ✅ 权限控制正常

### 2. 性能标准

- API响应时间 < 500ms
- 页面加载时间 < 3秒
- 并发用户数 > 100
- 系统可用性 > 99%

### 3. 质量标准

- 代码覆盖率 > 80%
- 无严重bug
- 文档完整
- 用户满意度 > 90%

## 版本信息
- 版本: v1.0
- 创建日期: 2026-06-01
- 作者: AI助手
